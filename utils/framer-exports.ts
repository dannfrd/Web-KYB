// Export komponen Framer Motion yang dibutuhkan
// Import secara eksplisit dan re-export untuk menghindari "export *"
import { AnimatePresence, domAnimation, LazyMotion, m, motion } from 'framer-motion';

export {
    AnimatePresence, domAnimation,
    LazyMotion, m, motion
};

